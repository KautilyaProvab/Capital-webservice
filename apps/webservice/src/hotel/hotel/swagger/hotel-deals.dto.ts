
import { ApiProperty } from "@nestjs/swagger";
import { IsNotEmpty, IsString } from "class-validator";

export class HotelDealsDto {
    
    // @ApiProperty({example: 'test'})
    // @IsNotEmpty()
    // @IsString()
    // code: string;
    
}

export class HotelDealsDao {
    
}